package fi.tamk.tiko.trainschedules;

/**
 * Interface to share data between two fragments or activities.
 */
public interface ChosenStation {
    void itemSelected(String stationName,String  stationCode);
}
